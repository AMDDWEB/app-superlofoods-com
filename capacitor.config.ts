import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.rameysmarketplace.com',
  appName: 'app-rameysmarketplace-com',
  webDir: 'dist'
};

export default config;
